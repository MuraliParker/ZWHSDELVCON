# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.6"
metadata:
  name: whs.delivery.consolidation.whsdeliveryconsolidation
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: DW1
          url: http://parkerdw1vm:8001
        app:
          name: ZWHSDELVCON
          description: WHS_E011 Delivery Consolidation
          package: ZFIORI
          transport: DW1K900362
        exclude:
          - /test/
